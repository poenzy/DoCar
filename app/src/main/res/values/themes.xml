<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DoCar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="RatingBarYellow" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/neutral_divider</item>
        <item name="colorControlActivated">@color/secondary_yellow</item>
    </style>

    <style name="Theme.DoCar" parent="Base.Theme.DoCar" />
</resources>